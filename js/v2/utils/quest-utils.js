/**
 * @file utils/quest-utils.js
 *
 * Pure utility functions for quest data.
 *
 * Functions planned (Phase 8):
 *   - buildObjectiveTree(quest) → nested objective structure
 *   - resolveCollectibleEntities(room, quest) → matching entity IDs
 *   - isQuestComplete(quest) → boolean
 *   - getQuestProgress(quest) → { completed, total, percent }
 *
 * No dependencies. No side effects.
 */
