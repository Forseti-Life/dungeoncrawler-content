/**
 * @file utils/spell-utils.js
 *
 * Pure utility functions for spell data.
 *
 * Functions planned (Phase 5):
 *   - getAvailableSpells(character, actionCost) → spell list
 *   - getSpellActionCost(spell) → number (1|2|3)
 *   - getSpellRanks(spell) → available ranks for heightening
 *   - canCast(character, spell, rank) → boolean + reason
 *
 * No dependencies. No side effects.
 */
