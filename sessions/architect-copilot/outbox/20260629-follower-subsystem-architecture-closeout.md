- Status: done
- Summary: Closed inbox item `20260622-follower-subsystem-architecture` after validating end-to-end completion of the follower actor unification stream and canonical subsystem contract rollout.

## Delivered
- Confirmed completion of unified follower subsystem architecture and implementation stream:
  1. Canonical follower taxonomy/contract for familiar, animal companion, construct companion, eidolon.
  2. Shared subsystem boundary (`FollowerSubsystemService`) for sheet projection, runtime projection, actor-record persistence, and creation-contract normalization.
  3. Dedicated follower detail route/sheet path and caller convergence onto canonical subsystem contracts.
  4. Runtime authority hardening (persisted actor-record requirements, identity validation, and removal of reconstruction fallback paths).
  5. Migration/backfill and conformance coverage completion (U1-U7 stream).
- Normalized item status to `done` / `current_phase: completed` in inbox status.

## Next Action
1. Continue monolith queue progression with the next pending item after this closeout.
