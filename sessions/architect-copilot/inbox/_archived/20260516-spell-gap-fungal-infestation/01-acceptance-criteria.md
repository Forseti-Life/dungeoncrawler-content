# Acceptance Criteria

- Identify the authoritative source text and metadata for `fungal-infestation`.
- Determine whether the fix belongs in extractor logic, source-backed overrides, intermediary payload curation, or DB normalization strategy.
- Define the minimal safe change needed so the canonical DB row carries a full description and correct metadata.
- Note whether any legacy underscore duplicate row should also be synchronized or explicitly ignored.
