# Problem Statement

The full spell-library audit found that the canonical DB spell row for `invoke-spirits` from `secrets_of_magic` still has an incomplete or snippet-only description in the live spell registry.

This is no longer blocking the wizard rank 0/1 arcane flow, but it remains part of the broader spell-system source-of-truth cleanup.
