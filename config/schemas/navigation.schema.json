{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://dungeoncrawler.life/schemas/navigation.schema.json",
  "title": "Navigation Action Schema",
  "description": "Canonical navigate_to_location action payload after server normalization.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "campaign_id", "actor_id", "source_room_id", "target_room_id", "transition_mode", "type", "name", "details", "state_changes"],
  "properties": {
    "schema_version": {
      "type": "string",
      "enum": ["navigation-action-v2"]
    },
    "campaign_id": {
      "type": "integer",
      "minimum": 1
    },
    "actor_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 255,
      "pattern": "^[a-z0-9][a-z0-9_-]*$"
    },
    "source_room_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 255,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
    },
    "target_room_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 255,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
    },
    "transition_mode": {
      "type": "string",
      "enum": ["in_session"]
    },
    "type": {
      "type": "string",
      "enum": ["navigate_to_location"]
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 255
    },
    "details": {
      "type": "object",
      "additionalProperties": false,
      "required": ["destination", "destination_description", "travel_type", "estimated_distance"],
      "properties": {
        "destination": {
          "type": "string",
          "minLength": 1,
          "maxLength": 255
        },
        "destination_room_id": {
          "type": "string",
          "minLength": 1,
          "maxLength": 255
        },
        "destination_description": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1000
        },
        "travel_type": {
          "type": "string",
          "enum": ["walk", "ride", "teleport", "climb", "swim", "fly"]
        },
        "estimated_distance": {
          "type": "string",
          "enum": ["adjacent", "short", "medium", "long"]
        }
      }
    },
    "state_changes": {
      "type": "object",
      "additionalProperties": false,
      "required": ["character", "room"],
      "properties": {
        "character": {
          "type": "object"
        },
        "room": {
          "type": "object"
        }
      }
    }
  }
}
