# GitHub PAT Provisioning Request — ISSUE-022

**Status:** needs-board  
**Severity:** Medium (non-blocking)  
**Due:** Before end of release cycle  

## Problem
GitHub PAT in `/home/ubuntu/github.token` is invalid (returns HTTP 401).
- Current token: `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`
- Impact: Health checks report GitHub Actions status as "unknown"
- Workaround active: Coordinated push uses local signoff files (functional)

## What Board Needs to Do

1. Generate new GitHub Personal Access Token at: https://github.com/settings/tokens
2. Repository: `keithaumiller/forseti.life`
3. Scopes: `repo`, `workflow`, `public_repo`
4. Provide token value to CEO

## How CEO Will Install

Once Board provides token:
```bash
echo "TOKEN_VALUE" > /home/ubuntu/github.token
```

Then verify:
```bash
GH_TOKEN=$(cat /home/ubuntu/github.token) gh api /user
# Expected: 200 OK with user JSON (not 401 error)
```

## Implementation
- No code changes needed
- No reboot needed
- Automatic integration via existing crontab
- Health check will use it immediately

## Next Steps
1. ✋ Awaiting Board: Generate and provide new PAT
2. 🔄 CEO: Store token and verify
3. ✅ System: Health checks report full GitHub Actions status

---

**Needs from:** Board (GitHub admin access to keithaumiller/forseti.life)

